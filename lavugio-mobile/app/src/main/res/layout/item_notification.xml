<?xml version="1.0" encoding="utf-8"?>
<!-- Odgovara <app-notification-item> komponenti -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="#FEFAE0">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Naslov notifikacije — odgovara <h3> -->
        <TextView
            android:id="@+id/textNotificationTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#283618"
            android:layout_marginBottom="4dp" />

        <!-- Tekst notifikacije — odgovara <p> -->
        <TextView
            android:id="@+id/textNotificationBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#283618"
            android:layout_marginBottom="8dp" />

        <!-- Datum — odgovara <small> sa date pipe -->
        <TextView
            android:id="@+id/textNotificationDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:textColor="#606C38" />

    </LinearLayout>

</androidx.cardview.widget.CardView>